# RenderDoc MCP Test Report

Date: 2026-04-11
Workspace: `D:\renderdoc\test`

## Objective

Validate the local RenderDoc MCP integration against a known-good OpenGL sample that we can build, run, capture, and inspect end-to-end.

## Test Asset

- Sample project: `renderdoc_mcp_opengl_smoke`
- Source: `src/renderdoc_mcp_opengl_smoke.cpp`
- Build file: `CMakeLists.txt`
- Rendering content:
  - draw 1: solid color triangle
  - draw 2: textured quad using a procedural 4x4 checker texture

## Environment

- OS: Windows
- GPU reported by sample: `AMD Radeon(TM) Graphics`
- Standalone run:
  - `GL_VERSION=3.3.0 Core Profile Context 25.10.30.02.250923`
  - `Renderer path=modern-shader`
- Injected CLI capture run:
  - `GL_VERSION=4.6.0 Core Profile Context 25.10.30.02.250923`

## Test Cases

| ID | Case | Result | Notes |
|---|---|---|---|
| TC01 | Configure with `cmake -S . -B build` | PASS | Visual Studio 18 2026 generator selected automatically |
| TC02 | Build with `cmake --build build --config Release` | PASS | Output exe created under `build\bin\Release` |
| TC03 | Run sample directly | PASS | Program completed 240 frames cleanly |
| TC04 | MCP `capture_frame` on sample exe | PARTIAL | Capture file was created, but tool returned `Capture completed but file not found on disk` |
| TC05 | MCP post-capture analysis tools (`open_capture`, `get_capture_info`) | FAIL | Calls returned `Transport closed` |
| TC06 | CLI `info` on generated capture | PASS | Capture is valid and readable |
| TC07 | CLI `events` / `draws` | PASS | 4 events, 2 draw calls detected |
| TC08 | CLI `pipeline` / `shader` / `resources` | PASS | Shader IDs, swapchain target, and texture resource visible |
| TC09 | CLI `export-rt` visual verification | PASS | Exported PNG matches expected triangle + checkerboard output |

## Key Evidence

### Valid capture produced from MCP flow

- Capture generated by MCP tool:
  - `artifacts\renderdoc_mcp_smoke_frame189.rdc`
- CLI probe capture:
  - `artifacts\cli_capture_probe_frame198.rdc`

### CLI inspection summary for `renderdoc_mcp_smoke_frame189.rdc`

- API: `OpenGL`
- Total events: `4`
- Total draws: `2`
- Draw events:
  - EID `7`: 3 indices, 1 instance
  - EID `13`: 6 indices, 1 instance
- Resources include:
  - `Texture 63`, size `4x4`, format `R8G8B8A8_UNORM`
  - Swapchain color target `1000000000000000075`, size `944x601`

### Shader inspection on draw EID 13

- Vertex shader ID: `56`
- Pixel shader ID: `57`
- Pixel shader reflection shows a sampled texture uniform:
  - `uTexture`
  - `DescriptorSet(0), Binding(0), Location(0)`

### Visual verification

- Exported render target:
  - `artifacts\export_rt\rt_13_0.png`
- Expected content confirmed:
  - left side colored triangle
  - right side checkerboard textured quad
  - dark blue background

## Findings

### Finding 1

`capture_frame` can succeed in producing an `.rdc`, but still reports failure because the output file name is suffixed with `_frameNNN`.

Evidence:

- Requested path: `artifacts\renderdoc_mcp_smoke.rdc`
- Actual file created: `artifacts\renderdoc_mcp_smoke_frame189.rdc`
- Tool response: `Capture completed but file not found on disk`

Impact:

- False negative status returned to the caller
- Follow-on automation cannot reliably use the returned path

### Finding 2

Post-capture MCP analysis is currently blocked because analysis tool calls fail with `Transport closed`.

Evidence:

- `open_capture` -> `Transport closed`
- `get_capture_info` -> `Transport closed`

Impact:

- Core MCP analysis workflow is unusable after capture in the current local setup
- `get_bindings`, `get_shader`, `list_resources`, `export_render_target` could not be exercised through the MCP tool surface itself

### Finding 3

The underlying RenderDoc capture data is valid. The failure is in the MCP integration layer, not in the OpenGL sample.

Evidence:

- Sample runs successfully outside RenderDoc
- CLI successfully reads the generated `.rdc`
- CLI exports a correct render target image from the capture

## Recommended Next Steps

1. Fix `capture_frame` path reconciliation so it accepts RenderDoc's `_frameNNN.rdc` naming pattern and returns the actual created file path.
2. Investigate why the MCP server connection closes before `open_capture` and other analysis tools can run.
3. Once transport is stable, rerun the same sample and explicitly verify:
   - `open_capture`
   - `get_capture_info`
   - `list_draws`
   - `goto_event`
   - `get_pipeline_state`
   - `get_bindings`
   - `get_shader`
   - `list_resources`
   - `export_render_target`

## Artifacts

- Sample source: `src\renderdoc_mcp_opengl_smoke.cpp`
- Built exe: `build\bin\Release\renderdoc_mcp_opengl_smoke.exe`
- MCP capture: `artifacts\renderdoc_mcp_smoke_frame189.rdc`
- CLI capture probe: `artifacts\cli_capture_probe_frame198.rdc`
- Exported PNG: `artifacts\export_rt\rt_13_0.png`

## Retest After Upgrade To v0.3.1

Retest date: 2026-04-11

After upgrading to `renderdoc-mcp v0.3.1` and restarting Codex Desktop, the original blocking workflow bugs were no longer reproducible in the MCP server:

- `capture_frame` succeeded and returned valid JSON with:
  - `api = OpenGL`
  - `totalEvents = 4`
  - `totalDraws = 2`
  - `path = D:\renderdoc\test\artifacts\repro\mcp_capture_repro.rdc`
- Follow-on MCP analysis calls also succeeded:
  - `get_capture_info`
  - `list_draws`
  - `open_capture`
  - `goto_event`
  - `get_pipeline_state`
  - `get_bindings`
  - `get_shader`
  - `export_render_target`

Retest artifacts:

- MCP regression capture: `artifacts\repro\mcp_capture_repro.rdc`
- RenderDoc auto-suffixed copy: `artifacts\repro\mcp_capture_repro_frame188.rdc`
- MCP response log: `artifacts\repro\mcp_repro_result_v2.json`
- MCP analysis log: `artifacts\repro\mcp_analysis_result.json`
- Retest render target export: `artifacts\repro\renderdoc-mcp-export\rt_13_0.png`

## Advanced Scenario Coverage

Additional sample: `renderdoc_mcp_opengl_advanced`

- Source: `src\renderdoc_mcp_opengl_advanced.cpp`
- Built exe: `build\bin\Release\renderdoc_mcp_opengl_advanced.exe`

### Advanced Sample Features

- Offscreen FBO render pass with:
  - color texture attachment
  - depth-stencil renderbuffer
  - two depth-tested textured draws
- Backbuffer composite pass with:
  - two simultaneously bound textures in the fragment shader
- Final overlay pass with:
  - alpha blending enabled
- Vertex-stage uniform buffer block:
  - `SceneData`
  - matrix + tint data

### Advanced Capture Results

- Capture file:
  - `artifacts\advanced\advanced_capture.rdc`
- Auto-suffixed copy:
  - `artifacts\advanced\advanced_capture_frame185.rdc`
- Capture summary:
  - API: `OpenGL`
  - total events: `7`
  - total draws: `4`
- Draw events:
  - `19` offscreen scene draw 1
  - `23` offscreen scene draw 2
  - `41` backbuffer composite draw
  - `56` blended overlay draw

### Tools Verified In Advanced Scenario

- `capture_frame`
- `get_capture_info`
- `list_draws`
- `get_pass_statistics`
- `get_pass_attachments`
- `goto_event`
- `get_pipeline_state`
- `get_bindings`
- `list_cbuffers`
- `get_cbuffer_contents`
- `list_resources`
- `find_unused_targets`
- `pick_pixel`
- `pixel_history`
- `assert_count`
- `assert_clean`
- `assert_pixel`
- `export_render_target`

### Advanced Visual Verification

- Exported final render target:
  - `artifacts\advanced\renderdoc-mcp-export\rt_56_0.png`
- Expected content confirmed:
  - central offscreen-composited checker scene
  - visible alpha-blended white checker overlay in the upper-left region

### Advanced Findings

#### Finding A1

`list_passes` returned zero passes for the advanced capture, but `get_pass_statistics` reported two synthetic passes with valid dimensions and draw counts.

Evidence:

- `list_passes` -> `count = 0`
- `get_pass_statistics` -> `count = 2`
  - pass 1: `512x512`, `2` draws
  - pass 2: `1008x681`, `2` draws

Impact:

- Frame-structure workflows that depend on `list_passes` can under-report valid OpenGL render passes.

#### Finding A2

`get_cbuffer_contents` discovered the `SceneData` constant block correctly, but returned all-zero values for both scene draws.

Evidence:

- `list_cbuffers(stage=vs)` reported:
  - block name `SceneData`
  - byte size `80`
  - variable count `2`
- `get_cbuffer_contents(stage=vs, index=0)` at event `19`:
  - `uMvp` = all zeros
  - `uTint` = all zeros
- `get_cbuffer_contents(stage=vs, index=0)` at event `23`:
  - same all-zero result

Why this is suspicious:

- The sample visibly renders two transformed, tinted quads in the offscreen pass.
- Those draws depend on non-zero matrix and tint data written into the UBO immediately before each draw.

Impact:

- Constant-buffer inspection for OpenGL UBO-backed shaders may currently be unreliable even when reflection metadata is present.

#### Finding A3

`get_bindings` on the composite draw detected both fragment textures, but reported the same bind point for both.

Evidence:

- Event `41` fragment shader resources:
  - `uSceneTexture`
  - `uLutTexture`
- Reported bind points:
  - both shown as `bindPoint = 0`

Why this is suspicious:

- The sample binds the two samplers to texture units `0` and `1` before the composite draw.

Impact:

- OpenGL multi-texture binding inspection may be losing slot accuracy even when resource names are found.

#### Finding A4

`debug_pixel` did not produce a fragment debug trace for an overlay pixel that `pixel_history` showed was modified by the overlay draw.

Evidence:

- `pixel_history(x=201, y=170)` showed modifications from:
  - event `32` background write
  - event `56` overlay write
- `debug_pixel(eventId=56, x=201, y=170, mode=summary)` returned:
  - `No debuggable fragment at (201,170) for event 56`

Impact:

- Pixel debugging may not be consistently available for this OpenGL/blended fragment scenario.

### Advanced Artifacts

- Capture log:
  - `artifacts\advanced\advanced_capture_log.json`
- Analysis log:
  - `artifacts\advanced\advanced_analysis_log.json`
- Deep checks:
  - `artifacts\advanced\advanced_deep_checks.json`

## Rebuild And Reinstall Validation

Retest date: 2026-04-13

Validation flow:

1. Rebuild `D:\renderdoc\renderdoc-mcp` with `cmake --build build --config Release`
2. Repackage the freshly built `Release` binaries and reinstall with `install-codex.ps1`
3. Re-run a direct stdio JSON-RPC regression against the newly installed:
   - `C:\Users\Administrator\.codex\vendor_imports\renderdoc-mcp\bin\renderdoc-mcp.exe`
4. Capture a fresh frame from `renderdoc_mcp_opengl_advanced.exe`
5. Verify the previously failing OpenGL inspection points

### Reinstall Result

- Installed binary timestamp:
  - `renderdoc-mcp.exe` -> `2026-04-13 10:22:31`
- Installed location:
  - `C:\Users\Administrator\.codex\vendor_imports\renderdoc-mcp\bin`
- New capture generated successfully:
  - `artifacts\advanced\retest-after-install\advanced_capture_after_install.rdc`
  - `artifacts\advanced\retest-after-install\advanced_capture_after_install_frame183.rdc`

### Regression Result

All three previously observed OpenGL issues were no longer reproducible after rebuilding from source and reinstalling:

- `list_passes` returned `2` synthetic passes
- `get_cbuffer_contents` returned real UBO values
  - `uMvp` contained non-zero matrix data
  - `uTint` = `[1.0, 0.92, 0.86, 1.0]`
- `get_bindings` on the composite draw returned correct texture slots
  - `uSceneTexture` -> `bindPoint = 0`
  - `uLutTexture` -> `bindPoint = 1`

### Rebuild Retest Artifact

- Direct MCP regression log:
  - `artifacts\advanced\retest-after-install\retest_after_install_20260413.json`

## Project Test Suite Check

After the rebuild, `ctest -C Release --output-on-failure` was executed in `D:\renderdoc\renderdoc-mcp\build`.

- Result: `180 / 181` tests passed
- One integration test failed:
  - `WorkflowTest.FullDebugWorkflow`

### Failing Test Note

The failing assertion expects `list_events` to return a top-level array. The current server returns an object with this shape instead:

- `{"count": 7, "events": [...]}` 

This means the remaining failure is in the integration test expectation, not in the three OpenGL fixes verified above.

## Extended Tool Coverage

Retest date: 2026-04-13

This round focused on the broader MCP tool surface beyond capture, pass inspection, and basic resource queries.

### Added Test Harness

- Reusable script:
  - `scripts\run_extended_mcp_checks.py`
- Purpose:
  - drive `renderdoc-mcp.exe` over stdio
  - capture two fresh advanced-sample frames
  - run shader, resource, and diff tool checks
  - write a structured JSON report under `artifacts\extended`

### Tools Verified In Extended Coverage

The following tools worked as expected on the advanced OpenGL sample:

- `list_events`
- `get_draw_info`
- `get_shader`
  - VS reflection
  - VS disassembly
  - PS reflection
- `list_shaders`
- `search_shaders`
- `get_texture_stats`
- `get_resource_usage`
- `export_texture`
- `diff_open`
- `diff_summary`
- `diff_draws`
- `diff_resources`
- `diff_framebuffer`
- `diff_close`

### Diff Validation Result

Two fresh captures from the same advanced sample were compared and were identical:

- draw diff:
  - `modified = 0`
  - `added = 0`
  - `deleted = 0`
- resource diff:
  - `modified = 0`
  - `added = 0`
  - `deleted = 0`
- framebuffer diff:
  - `diffPixels = 0`
  - `diffRatio = 0.0`

This confirms the advanced sample is stable enough to use as a diff-regression fixture.

### Extended Findings

#### Finding E1

`debug_vertex` did not succeed on the tested OpenGL draws.

Evidence:

- advanced sample:
  - event `19` -> `Cannot debug vertex 0 at event 19`
- smoke sample cross-check:
  - event `7` -> `Cannot debug vertex 0 at event 7`

Impact:

- Vertex shader single-invocation debugging is currently not available on the tested OpenGL captures.

#### Finding E2

`debug_pixel` did not succeed even for pixels that were confirmed by `pixel_history` to have been modified by the target draw.

Evidence:

- advanced sample confirmed-hit pixels:
  - composite draw event `41`, pixel `(504,340)`
    - `pixel_history` shows modification by event `41`
    - `debug_pixel` -> `No debuggable fragment at (504,340) for event 41`
  - overlay draw event `56`, pixel `(201,170)`
    - `pixel_history` shows modification by event `56`
    - `debug_pixel` -> `No debuggable fragment at (201,170) for event 56`
- smoke sample confirmed-hit pixels:
  - triangle draw event `7`, pixel `(160,400)`
    - `pixel_history` shows modification by event `7`
    - `debug_pixel` -> `No debuggable fragment at (160,400) for event 7`
  - textured quad draw event `13`, pixel `(560,320)`
    - `pixel_history` shows modification by event `13`
    - `debug_pixel` -> `No debuggable fragment at (560,320) for event 13`

Impact:

- Fragment-level shader debugging appears unavailable on the tested OpenGL captures even when pixel ownership is confirmed.

### Extended Conclusion

Follow-up root-cause analysis indicates that Findings `E1` and `E2` are not `renderdoc-mcp` logic bugs.

The more likely cause is a RenderDoc OpenGL shader-debugging limitation:

- the tested shaders are reported as debuggable
- `renderdoc-mcp` successfully reaches RenderDoc's debug entrypoints
- RenderDoc still returns no usable debug trace for the tested OpenGL vertex and fragment cases

This means the observed failures are best classified as RenderDoc replay / software-debugger limitations on the tested OpenGL path, not as incorrect MCP request routing or response parsing.

## Debug Error Message Retest

Retest date: 2026-04-13

After the above analysis, `renderdoc-mcp` was rebuilt from source, reinstalled into Codex, and retested specifically to validate improved debug failure messages.

### Installation Used For Retest

- Installed binary:
  - `C:\Users\Administrator\.codex\vendor_imports\renderdoc-mcp\bin\renderdoc-mcp.exe`
- Installed binary timestamp:
  - `2026-04-13 10:55:31`

### Retest Goal

Verify that OpenGL debug failures now distinguish between:

1. shader not debuggable
2. shader marked debuggable, but RenderDoc failing to produce a debug trace

### Retest Result

The updated binary now reports the tested OpenGL failures much more clearly.

Verified behavior on both smoke and advanced samples:

- `debug_vertex` now reports:
  - the shader is marked as debuggable
  - RenderDoc could not produce a debug trace
  - this can happen with OpenGL features not fully supported by the software shader debugger
- `debug_pixel` now reports the same underlying explanation instead of the older generic `No debuggable fragment` wording

This confirms that the current user-facing diagnosis is now aligned with the observed root cause.

### Example Messages Observed

- smoke sample vertex debug:
  - `Cannot debug vertex 0 at event 7. The shader is marked as debuggable, but RenderDoc could not produce a debug trace. This can happen with certain OpenGL shader features that are not fully supported by the software shader debugger.`
- advanced sample pixel debug:
  - `No fragment hit at (504,340) for event 41. The shader is marked as debuggable, but RenderDoc could not produce a debug trace. This can happen with certain OpenGL shader features that are not fully supported by the software shader debugger.`

### Interpretation

For the tested `#version 330 core` OpenGL captures, the effective conclusion is now:

- this is not a `renderdoc-mcp` functional bug
- the shader is not being rejected as legacy / non-debuggable
- RenderDoc is instead failing at the later stage where it should provide an executable debug trace

One wording detail remains: in the tested fragment cases, the message still starts with `No fragment hit` even when `pixel_history` confirms the draw touched that pixel. The added explanation after that prefix is correct, but the prefix itself could still be refined if stricter wording is desired.

### Retest Artifact

- Installed-binary debug message verification:
  - `artifacts\debug-message-retest\debug_message_retest_20260413.json`

### Extended Artifacts

- Extended tool-run summary:
  - `artifacts\extended\extended_mcp_checks.json`
- Extended captures:
  - `artifacts\extended\advanced_extended_a.rdc`
  - `artifacts\extended\advanced_extended_b.rdc`
- Extended diff image:
  - `artifacts\extended\advanced_extended_diff.png`
- Confirmed-hit pixel evidence for advanced sample:
  - `artifacts\extended\advanced_debug_pixel_confirmation.json`
- Confirmed-hit pixel evidence for smoke sample:
  - `artifacts\extended\smoke-debug-check\smoke_debug_confirmed_hits.json`
